# Workload and capacity report bilingual batch

## Source and state before editing

- Article key: `analytics-reporting-attribution/workload-capacity-report-guide.md`. Published routes: `https://help.hellotext.com/es/guia-reporte-carga-capacidad` and `https://help.hellotext.com/workload-capacity-report-guide`; inventory records HTTP 200 for both and `progress.csv` marks the pair `pending`.
- Complete pre-edit bodies and shared stub are preserved byte for byte under `originals/workload-capacity-report-guide/`. SHA-256: Spanish `a94c1e3aa8bbe40daafc4996f290dfedbdec1e6aa98de291af70b40a3f7ef547`; English `18978830101a14743a291445864f8e45fc279c83c14b4a6455feea8678d5c722`; stub `963293901078a68852be5a8417f767058febc244f65206699f950ca604c1e3f5`.
- A related diagnostic guide has a contradictory Operational Pressure sentence; its complete current Spanish and English bodies were also preserved under `originals/workload-capacity-report-guide/related/` before any correction. SHA-256: Spanish `5fc6b83725e8749b85fb8c5c50809fa4bffb5285044b67303a4b32b61a31a21a`; English `b0c631aac7b6d38cf28444aa1a59292c11c72639fb719a227a5833537d24d3b1`.
- Start from Help `origin/main` at `910ef421219cf08a0ab6e39efd00fb459a8f3bd5` on `codex/help-workload-capacity-report`. Preserve titles, slugs, links, languages, navigation and publication state. Rails source review uses `origin/master` at `6ea4ccbb208ad53f3c061691f3737b1e1902e259`.

## Reader task and article-specific plan

The reader needs to distinguish work performed during the chosen period, the percentage of capacity consumed, and live queue signals before changing Inbox capacity or staffing.

1. Correct **Manejadas / Handled**: count distinct conversations with a qualifying human handling interval overlapping the chosen period, not first replies. Replies, internal notes and human closes can start handling during an active session. One conversation can appear in more than one daily bucket or teammate/team row, so do not use the Handled minus Resolved gap as a backlog count.
2. Correct **Carga activa / Active load** to a percentage of recorded handling time over available session capacity. Explain its relationship to Capacity pressure, and clarify that **Concurrencia / Avg. concurrent** averages simultaneous handling during active work. Use one short numeric session example to distinguish capacity, active time and concurrency; it is illustrative, not a product result.
3. Clarify **Resueltas / Resolved** as human resolution, and **Asignadas / Assigned** and **Transferidas a / Transferred to** as handling overlapping the selected period under an ownership interval. Assigned means no handoff from another teammate for that interval; the teammate need not be the first human owner ever, and assignment/transfer can predate the chosen period.
4. Correct **Presión operativa / Operational Pressure**: Unanswered, Oldest waiting and SLA risk inspect current unresolved response obligations, while Utilization and Concurrent use selected-period work; Burn combines both. Update date guidance, interpretation, action table and troubleshooting accordingly. Correct the directly linked diagnostic guide's contradictory date-table row and operations sentence in both languages, after its independent full review.
5. Keep the existing navigation and related links. A screenshot would show one private queue state without explaining the mixed time bases or ratio; the numeric example and a short textual date example should answer the reader's questions without a capture. No customer-facing message example is involved.
6. Revise Spanish first and adapt English. Re-read current sources before saving; build and verify both complete articles at desktop and mobile, links, headings and metadata. Mark local verification only after checks. Rails [PR #6010](https://github.com/hellotext/hellotext/pull/6010) corrects the corresponding tooltips and must pass its checks and merge before the Help PR. Record merges, normal deployment and public routes separately from the local build.

## Checkpoint

Both workload translations were revised, followed by the two linked diagnostic translations. A second source review verified the 60-minute capacity example, the ownership and handling definitions, current unanswered SLA cycles, and team capacity attribution. The complete bilingual bodies preserve the original 15 workload and 21 diagnostic headings per language, all 9 and 15 Liquid link targets respectively, their tables, metadata stub, language routes, and publication state. No image is added because the comparison and numeric example explain these measures more clearly than a private queue capture.

The production-mode Jekyll build and security-header verification passed. Local HTTP and browser review covered all four routes in Spanish and English at the default 1280 px desktop viewport and 390 × 844 mobile viewport. The edited date table and workload example were inspected at both sizes; the document width matched the viewport, with no horizontal overflow. `git diff --check` passed. Help PR, merge, and public deployment verification are still pending.

The content was committed as `dc8159b3e2cf39eba913c6ed048e60aff72345f0` after a clean staged diff and final production-mode build. The workload pair and the linked diagnostic correction are recorded as locally verified in `progress.csv`. Rails PR #6010 must finish checks and merge before this Help branch is pushed and proposed; neither local verification nor this record claims publication.

Rails PR #6010 failed several CI shards because the base `master` revision had removed `Business::Provisioner`'s already-provisioned guard, causing duplicate default email themes. Its locale-only diff is unrelated to the failure. Separate Rails [PR #6011](https://github.com/hellotext/hellotext/pull/6011) restores the previous single-method implementation; the existing focused specs passed (26 examples) and its full CI is pending. Resolve and merge #6011 with all checks green, update #6010 onto the fixed master, wait for its checks and merge, then propose this Help branch. Do not treat a retry of the unchanged failing shards as a fix.
