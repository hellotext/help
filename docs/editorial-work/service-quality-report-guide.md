# Service quality report guide batch

## Source and current state

- Article key: `analytics-reporting-attribution/service-quality-report-guide.md`; public routes are `https://help.hellotext.com/es/guia-reporte-calidad-servicio` and `https://help.hellotext.com/service-quality-report-guide`. Both returned HTTP 200 on 2026-09-26.
- The complete current Spanish and English bodies and shared stub were preserved byte for byte under `originals/service-quality-report-guide/` before editing. The body SHA-256 values match `inventory.csv`: Spanish `798a6bcd5ed78076e72eb37ff79488334cde6f7d39f3270bb327cccbddc07d4c`; English `816047216a38d178a5d15c4331be714c90e1db4de2fdc99c3c56c9683d1bfb2f`. The shared stub is `566e59dd7db5f663a97718ee75c89f75a96316573847300be60a466148ccff7f`.
- Each original body has 13 Markdown headings and nine Liquid article links; the seven related-guide links resolve. `progress.csv` marks this pair `pending`. The branch starts from `origin/main` merge commit `87525b6e838b29ee3f9b31ce572e02d17e28f7cd`. Preserve titles, slugs, language pairing, existing destinations, navigation placement and publication state.

## Reader task and article-specific plan

The reader selects a Service quality metric and period, interprets its trend, breakdown and response-time widgets, then checks representative conversations before changing a response policy, routing rule or team capacity.

1. Audit the complete Spanish guide against current Rails report calculators, widgets, section registry and locale labels. Resolve the preliminary source findings that **Resolved by team** is a percentage rather than a count, the opening uses an overly broad conversation-start date rule, and the current report may show a Customer Satisfaction widget. Verify exact source revision and user-facing terms before editing.
2. Distinguish the date basis of lifecycle headline metrics, SLA and survey/agent widgets as implemented. Explain denominators and what an unanswered or recent conversation means without promising that every report result stays assigned to its first day. Replace the vague “service-analysis window” sentence with a concrete, source-supported explanation.
3. Add one short diagnostic path after the metric or action section: select SLA compliance, compare one breakdown, use response-time distribution only as wait context (not an SLA rate), and inspect representative conversations. Expand FCR at its first mention and link the existing Workload & capacity guide where the text directs readers to the current queue.
4. Adapt the complete verified Spanish result in English. Preserve all existing links and add only the specific useful guide link. A focused localized screenshot of the real metric selector and breakdown would help readers orient themselves, but the Mac remained locked at 11:28 UTC. Do not substitute a browser JPEG, fabricated chart or unrelated report. Reassess native capture after the source correction; record an outstanding visual enhancement if text is otherwise complete.
5. Re-read both sources before saving. Run the Help build and security-header check, verify the full rendered ES/EN pages at desktop and mobile widths, and record the verifying content commit for any previously completed linked guide. Keep this pair `pending` until the native visual capture and full review are complete. Record PR merge and public verification separately.

## Checkpoint

The original bilingual article and stub are preserved. The source audit used Rails `origin/master` at `6ea4ccbb208ad53f3c061691f3737b1e1902e259`: `Report::Calculator::ServiceQuality::Base`, `ResolvedByTeam`, `SlaCompliance`, `Report::Widget::ServiceQuality::{CustomerSatisfaction,ResponseTimeDistribution,ResolutionPath}`, and the agent-performance sections. The prior article incorrectly assigned all results to conversation start, described Resolved by team as a count, omitted Customer satisfaction, described the last response bucket as strictly greater than 10 minutes, and treated AI resolution time as ownership time. The corrected Spanish guide now explains each metric's date and denominator, the CSAT widget, the response-cycle samples, role-specific agent columns and a short SLA investigation. English was adapted from the complete Spanish result. All nine original Liquid links per locale remain in order; the two added Workload & capacity links resolve to an existing guide.

The linked Data completeness guide had the same overbroad date rule. Its complete earlier ES/EN bodies were preserved under `originals/service-quality-report-guide/related/` (SHA-256 Spanish `025c2b66b9878e61c17de46800f1d534d98e0005e56ca839dade5f7a42a37763`, English `ec71a96928a812758b27846cb1c1ee380978edb4169c210a17145ec652556a58`). Exactly one date-table row was corrected per locale; all 21 headings and 15 Liquid links remain unchanged.

Content correction commit: `78ef2a9`. The Help production build and `script/verify_security_headers.rb` passed on 2026-09-26. Both generated Service quality routes have 14 Markdown headings and 11 Liquid links; original links remain in order and all source targets exist. The complete ES/EN pages, including the action table, rendered locally at 1280px and 390px with no horizontal overflow or browser errors. The revised Data completeness row wrapped within the 358px mobile table in both languages. The plain local server requires `.html` URLs and does not emulate production's extensionless routes.

No native Service quality capture exists in the Help assets. The Mac was still locked at the most recent recheck, so its native screenshot surface was unavailable; in-app browser screenshots are unsuitable as published assets under the shared PNG Display P3 and 2× provenance rule. No screenshot was added or implied. The next Service quality batch must capture a focused real report selector/breakdown and any other reader-helpful widget with demonstration data in both languages, omit the cursor unless intentionally enlarged over a link or button, verify privacy/provenance and rerun the complete bilingual review. This pair stays `pending` in `progress.csv`; this batch covers a verified factual correction, not final visual completion.

## Public correction verification (2026-09-26)

Help [PR 96](https://github.com/hellotext/help/pull/96) merged the correction into `main` as merge commit `7505a519f6d27608da84079fd23ef1af3a3356d1` at 11:51 UTC. Its Build, Aikido Security, Netlify deploy preview and header checks passed. The merge commit has parents `87525b6` and `b7e209d`; `git merge-base --is-ancestor 78ef2a9 origin/main` succeeded after merge, confirming the ledger's content commit remains reachable. The [main Build run](https://github.com/hellotext/help/actions/runs/36240145124) passed.

At 11:53 UTC, the public [Spanish Service quality guide](https://help.hellotext.com/es/guia-reporte-calidad-servicio), [English guide](https://help.hellotext.com/service-quality-report-guide), and linked Spanish and English Data completeness guides all returned HTTP 200 with the corrected text. This verifies the normal Netlify publication of the factual correction. Native screenshots and the final Service quality editorial pass remain pending.
