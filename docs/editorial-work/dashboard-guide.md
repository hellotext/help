# Dashboard guide batch

## Source and current state

- Article key: `analytics-reporting-attribution/dashboard-guide.md`; Spanish and English published routes are `https://help.hellotext.com/es/guia-dashboard` and `https://help.hellotext.com/dashboard-guide`.
- At batch start, `progress.csv` recorded this pair as `pending`. The inventory's 2026-09-25 original SHA-256 values are Spanish `c22ca1802d0a4373c09b91e021b49eaed2dec97506a865cdc74f9854dbe63781` and English `b5aad25f2bdc33d0a11acb4c99d3f861219e6d173076ced09f28f046448b8a6d`; later main commits changed both bodies before this resumed batch.
- Before editing, the Spanish body, English body, and shared stub were copied byte for byte to `originals/dashboard-guide/`. Preserve titles, descriptions, slugs, existing links, both languages, and published state. The current bodies have the same 13 headings and 11 Liquid link targets per language, and no figures.
- The existing article branch `codex/help-editorial-dashboard-guide` was clean and fast-forwarded to `origin/main` at `a674158` after confirming Help PR 85 was merged and no Help PR remained open.

## Reader task and focused plan

The reader uses the Dashboard to notice a change, interpret its fixed 14-day overview and tracked actions, inspect scheduled or sent campaigns, and choose a detailed report. Correct any claims that differ from the current Rails implementation, starting with the campaign report's event-date grouping, the overview's zero-value behavior, the calendar's displayed range and interaction, and the complete set of report cards. Review the whole Spanish article first, then adapt the English article without changing its structure or existing link targets. Add direct guide links only where they help the reader move from the Dashboard to the named report or resolve a documented data gap.

Three potential figures answer distinct interface questions: (1) the 14-day KPI and benchmark cluster after the overview explanation; (2) the Actions table with safe demonstration event rows after the column explanation; and (3) the campaign calendar with synthetic scheduled and sent entries after its navigation instructions. A report-card crop is useful only if the actual Dashboard makes navigation unclear. Do not capture conditional account notices, private customer records, or a populated activity list. Use the real localized interface with a separate guarded synthetic fixture, crop each control or widget tightly, and save native Display P3 PNGs at 2× or higher for desktop and mobile. Every published figure must be static, unlinked, cursor-free by default, and readable inline.

## Source evidence and capture decision

- `BusinessesController#show` provides the Dashboard. `Dashboard::OverviewsController` and `Dashboard::ActionsController` use the current business day and previous 13 days in the business timezone. The card's default comparison uses the previous 14 days, but focusing a chart point temporarily shows that day's value and change from the previous day (`app/views/dashboard/overviews/_chart.html.erb:32-64`, `app/javascript/controllers/dashboard/overview_controller.js:32-42,68-79`). When every day is zero, the chart line is illustrative rather than measured activity (`app/helpers/dashboard/overviews_helper.rb:36-49`). `Calendar::CampaignDates` and the Dashboard view show two Monday-to-Sunday weeks in the user's timezone, falling back to the business timezone. A later recheck of Rails `26742adc0c4a848cc58ab6504cd46f28df769401` found three Business Performance and two Operations reports (`db/seeds/reports.rb:22-125,418-514`), matching the current five-row table; the earlier Channel performance note was stale. Main has also corrected the campaign detail date claim to use each event's date; preserve that wording.
- The isolated campaign-report capture database has four synthetic current calendar entries but no current `Dashboard::Metric`, attributed-revenue, or total-revenue rows for the 14-day cards or Actions. The empty Actions UI contains blurred illustrative samples, which must not be captioned as live data. A separate guarded synthetic Dashboard fixture is needed; no real account, contact, message, or campaign may be changed or sent. Before checking newly seeded widgets, clear the isolated cache or use a fresh namespace: the current Dashboard caches Actions for 15 minutes, conversations for 5 minutes, attributed revenue for 1 hour, and benchmark data for 5 minutes.
- At 2026-09-26 06:18 UTC the Mac was locked, so native screenshot capture was unavailable. The user was asked to unlock it while text and source work continue. No Dashboard screenshot has been captured or published in this batch.
- The first snapshots and plan were committed as `2cf1982` on `codex/help-editorial-dashboard-guide`, based on merged main `a674158`. They match the inventory's 2026-09-25 originals. The branch later merged `origin/main` at `2b8d94deb032c1baff6477f7aeb69ae0cd1b5ab0`, preserving two intervening bilingual article corrections. Byte-for-byte copies of the current pre-batch Spanish and English bodies are in `originals/dashboard-guide/pre-batch-current/`; their SHA-256 values are `20f44c608f8c247d29827dc240922d568474d506a8b4d8081188a1f7acc963f1` and `d7771867f41c2ba1dc2d26b9e90ea1f63828206830df94dc116a741e002521ae`. `progress.csv` was still `pending` at this point.
- The resumed bilingual edit explains the illustrative curve beside a dash, paginated **Cargar más** / **Load more** Actions, and the blurred example rows in the empty Actions state. It limits the hover instruction to a computer; the Rails calendar entry is a link, but whether its tooltip is useful on touch remains unverified, so the guide makes no touch-tooltip claim. The current five report rows and corrected campaign event-date paragraph are preserved. Evidence: Rails `26742adc0c4a848cc58ab6504cd46f28df769401`, `app/helpers/dashboard/overviews_helper.rb:36-49`, `app/controllers/dashboard/actions_controller.rb:13-31,43-57`, `app/views/dashboard/actions/index.turbo_stream.erb:11-26`, `app/views/dashboard/actions/_empty.html.erb`, and `app/components/ui/tooltip/tooltip.html.erb`.
- The macOS UI remained locked during the resumed batch, so a genuine native Display P3 capture could not be made. The proposed widget figures are optional orientation aids. The corrections above concern interpretation and loading behavior, which are explicit and readable in prose; no figure is required to understand them. No screenshot, message example, Rails fixture, contact, campaign, or outgoing message was created or changed. A later widget capture would require a separate safe fixture and full provenance review.

## Resumed batch verification (2026-09-26)

- `yarn build` passed in both languages after the final edit, including `script/verify_security_headers.rb`. `git diff --check` passed.
- Compared both complete locale bodies with the current pre-batch snapshots: all 13 headings and all 11 Liquid article links are preserved in order per language; every linked source file exists. The shared stub, titles, descriptions, slugs, and published state are unchanged.
- The built Spanish and English guides were reviewed in the in-app browser at 1280×800 and 390×844. The new text, Actions and report tables, navigation, and related guides render in both locales. At 390px, both tables fit the 358px article area without internal clipping or document overflow; the console reported no errors. The temporary viewport override was reset.
- The article and work record are locally verified. Content commit `a8ade1e049fe6e3fb1e2a590e0703939113e9e07` and ledger commit `e6bd0b633ace03a08c759bbaead584f9a59b2dab` record this stage; `progress.csv` marks the pair `local_verified` and points to the content commit. PR merge, Netlify production deployment, and public checks followed separately.

## Public verification (2026-09-26)

- [Help PR #102](https://github.com/hellotext/help/pull/102) merged into `main` with merge commit `6e05febb955f25c94669682533c7e85fc1447885` at 14:47:26 UTC. The merge has parents `90e5f5abbe8a39ca42e0afa5bbb03fe4a2bb95ad` and `e6bd0b633ace03a08c759bbaead584f9a59b2dab`; the verifying content commit is a direct ancestor of the latter. The Build, Aikido, Netlify preview, and header checks passed. Both preview locale routes returned HTTP 200 with the updated content.
- Automated review completed. It reported that the ledger's verifying commit might be unreachable, but the check used a revision different from the PR head. GitHub's commit parent relationship and local `git merge-base --is-ancestor` confirmed reachability; this evidence was replied to in the review thread before merging. No ledger rewrite or protection bypass was needed.
- The [post-merge main Build](https://github.com/hellotext/help/actions/runs/36249700989) passed. Netlify production deployment `6ab7daff60ed9d0008c4ad22` reached `ready` for merge commit `6e05febb955f25c94669682533c7e85fc1447885` and was published at 14:48:03.509 UTC.
- The public [Spanish guide](https://help.hellotext.com/es/guia-dashboard) and [English guide](https://help.hellotext.com/dashboard-guide) both returned HTTP 200 with the new empty-state and pagination guidance at approximately 14:48 UTC. The ledger continues to record the local editorial verifier rather than the deployment revision.

## Visual follow-up audit (2026-09-26)

The bilingual text and publication checks above remain valid, but the planned KPI, Actions, and calendar captures were not made. `progress.csv` now marks this pair `visual_pending`; retain the verified text commit and resume with a safe populated fixture and focused localized captures. See `visual-followup.md` for the shared queue.

## Calendar screenshot follow-up (2026-09-26)

- Started from clean `origin/main` `234533abe45b4727720d8867a3b055fdd9c71cbe` on `codex/dashboard-calendar-visual-followup`, with no open Help PR. The immediate pre-edit Spanish, English, and stub sources are preserved under `originals/dashboard-guide/pre-calendar-figure/`, with SHA-256 `20a983e2539bee86bc22d2615b292924fb8d0b218a7b7b129726d7edab802028`, `77c03a62a1d7ede67b22825bcc808ff4736ff527815b70685dd4e3d04ef5bbbb`, and `337775706595d46adbc87d18de1f114120de1b3b785d330fb99496ef912c842b` respectively.
- Reader question: how sent campaigns and an upcoming campaign appear in the Dashboard calendar. Insert two static, locale-specific close-ups after the calendar instructions, showing the sent day with **Ver más** / **See more** and the scheduled day. Preserve all surrounding text, 13 headings, 11 Liquid links per language, slugs, publication state, and the shared stub. The KPI and Actions visual follow-ups remain separate work.
- Used an isolated local Rails Dashboard at `http://localhost:3190/hellotext`, database `hellotext_editorial_capture_20260926`, with synthetic campaign entries only. The pending scheduled campaign has no audience, messages, or delivery jobs. English campaign titles were temporarily localized in that isolated database and restored after capture. No contact, message, or real campaign was changed or sent. The capture helper in `capture-tools/hellotext_capture_editor.swift` selected exact Chrome window ID `26501` and title `Panel de Enterprise | Hellotext`, checked each source rectangle, hid the cursor, and wrote genuine native Display P3 PNGs at 2× from a P3 display. No intermediate JPEG, resampling, or profile reassignment was used.

| Locale and crop | Native source rectangle (points) | PNG pixels | SHA-256 |
| --- | --- | --- | --- |
| Spanish sent | `-465,390,330,490` | 660 × 980 | `535e9a109b413f8b035a22f6118e90c5a623bf858a4fc438119381c0ba203ada` |
| Spanish scheduled | `-900,705,330,220` | 660 × 440 | `8e6b7d51c6991606f6a6d8d5c9a94774f1d62909c1aa4dd2564af043688407ed` |
| English sent | `-465,400,330,500` | 660 × 1000 | `2e0654c598438910abe9bb87bcebb282703e0ed4aea5be1f484f3252f1b73680` |
| English scheduled | `-900,720,330,220` | 660 × 440 | `d7194c7b44b083aeaf35d129b7a02f8dc956f7bdd123f16270023d11cf2a74f7` |

- Originals and per-image provenance are in `captures/dashboard-guide/`; byte-identical static assets are in `images/analytics-reporting-attribution/dashboard-guide/`. Full-size inspection confirmed locale, sent/scheduled statuses, synthetic names, absence of pointers and private data. `file` confirmed real PNG and `sips` confirmed embedded Display P3 and exact native dimensions. A broad first crop reduced labels too much in the Help column; it was discarded before publication, and only the four focused originals and assets remain.
- `PATH="$HOME/.rbenv/shims:$PATH" BUNDLE_PATH=vendor/bundle yarn build` and the security-header check passed. Both built locale routes contain the two figures, and the built root and `es/` PNG copies match the verified originals byte for byte. The shared stub remains byte-identical to its snapshot. `git diff --check` passed.
- Complete Spanish and English guides were visually reviewed at 1280 × 900 desktop and 390 × 844 mobile. The sent day, **Ver más** / **See more**, and the scheduled 11:00 card are legible inline. No figure is linked; the browser reports no horizontal overflow. The image frames cap at 330 CSS pixels; actual rendered image widths were 312px on desktop and 316px on mobile, preserving more than 2 source pixels per displayed CSS pixel. Accessible alt text and captions match the visible pixels; captions are visually hidden by the shared figure style and available to assistive technology.
- This calendar section is locally verified. Keep `progress.csv` at `visual_pending` because KPI and Actions figures still need their own synthetic fixture and review. PR checks, Netlify deployment, and public ES/EN asset checks are pending until the merge.
