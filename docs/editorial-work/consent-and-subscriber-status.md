# Consent and subscriber status bilingual batch

## Source and current state

- Article key: `audience/consent-and-subscriber-status.md`. Public routes: `https://help.hellotext.com/es/a-quien-puedo-escribirle` and `https://help.hellotext.com/who-can-i-message`, both listed as HTTP 200 in `inventory.csv`.
- The complete Spanish and English bodies and the shared stub were preserved byte for byte under `originals/consent-and-subscriber-status/` before editing. Their SHA-256 hashes are Spanish `de63ce550874b018d2d85b5342e3b359d83f0515f5d5f32aa37de8d5cb07d64e`, English `5f2e57aeb705ea9d7821744b2ba457a88f045c91115d07fa046ef932accd0976`, and stub `385de4543ba9e1168890ea40e520507369e39329349a91bc7c98d8da5f3193a1`. The locale body hashes match the inventory.
- Each original locale body has eight second-level headings and seven Liquid article links. `progress.csv` marks this pair `pending`; every existing `local_verified` ledger commit is an ancestor of branch base `79dc450041e93d873f3958072ed8af3e9d844393`. Preserve titles, slugs, the legal-review caveat, language pairing, links, navigation placement, and publication state.

## Reader task and article-specific plan

The reader decides whether a selected audience can receive a particular promotional message on a particular channel before importing or sending. The existing three-question framework is sound but needs one short worked decision example that applies subscription state, destination availability, and channel rules together.

1. Correct the current guide's overgeneralization about destination-level checks. Rails `26742adc0c4a848cc58ab6504cd46f28df769401` filters SMS/WhatsApp destinations by phone status and identifier-level unsubscribe (`app/models/contact/reachability_query.rb`), while email campaigns use a kept address and profile-level unsubscribe, without per-address consent in recipient selection (`app/models/contact/reachability_query.rb` and `app/models/message/effigy.rb`). Subscription/opt-out events record consent for current email, phone, and WhatsApp identifiers, rather than every possible channel (`app/models/marketing/consent/opt_in.rb` and `opt_out.rb`). Unconfirmed profiles can remain campaign candidates (`app/models/contact/audience_query.rb`). State these behaviors accurately without suggesting that product selection proves legal permission. Keep the legal-review caveat.
2. Immediately after **Disponibilidad no significa consentimiento** / **Reachability is not consent**, add a compact illustrative comparison for a promotional SMS campaign: a subscribed profile with a usable SMS number still needs sender/channel checks; a subscribed profile with an opted-out or invalid SMS number must not be sent on that destination; an unconfirmed profile with a valid phone is not established as eligible for marketing even if the product can select it. Do not depict any actual contact or assert that one check alone authorizes a send.
3. In the final checklist, direct the reader to use their own or authorized internal test profiles first, in line with the linked go-live checklist. Do not send any test message as part of this editorial batch.
4. Start with the complete Spanish body and adapt English. Keep the eight headings, seven Liquid links and their order, all three subscription states and their advice, metadata, and published state. At 390px, the original three-column state table extends its final cells beyond the article width and clips text; combine meaning and action into one column for a readable two-column table in both languages. No screenshot is planned: this article explains an eligibility decision, and a real interface capture would add little while risking customer identifiers. No message example is needed because it does not explain customer-facing copy.
5. Verify both complete local articles at desktop and mobile widths, build Help and security headers, inspect link targets and diff, then record the verifying content commit in `progress.csv`. Push and merge only after checks and review; record actual public verification separately.

## Checkpoint

Originals are preserved. The source audit is complete against Rails `26742adc0c4a848cc58ab6504cd46f28df769401` and the linked import and go-live guides. Both locale bodies now contain the channel-specific factual corrections, three-case example, internal test guidance, and a two-column version of the original three-state table. A second source review found no unsupported new claim. The shared stub, eight headings, seven Liquid link targets and their order, titles, slugs, and publication state are unchanged; all link target files exist.

Local verification on 2026-09-26: `yarn build` passed, including `script/verify_security_headers.rb`, after the final table revision; `git diff --check` passed. The complete Spanish and English pages rendered at 1280×800 and 390×844 in the in-app browser. Their corrected paragraphs, example, table, and related links remained readable. The two-column table fits within the 358px article area at 390px; neither page had document-level horizontal overflow or browser console errors. No screenshot or message asset was added because the guide explains a decision rather than a specific UI control or customer-facing message. No test message was sent. Verifying content commit: `50c987492608662a26e6b2bbfe2482ddbc4ec2ad`. The ledger records `local_verified`; PR merge and public verification are pending.
