# Campaign reporting batch

## Source and scope

- Article key: `analytics-reporting-attribution/campaign-reporting.md`.
- Spanish and English bodies and the shared stub were copied byte for byte to `originals/campaign-reporting/` before editing. The translated body hashes matched `inventory.csv` on 2026-09-26.
- Public routes: `https://help.hellotext.com/es/reportes-de-campaña` and `https://help.hellotext.com/campaign-reporting`. The inventory records the original published state; this batch is local only.
- Preserve titles, descriptions, slugs, existing links, language pairing, and publication state.

## Reader task and plan

The reader needs to choose the correct report period, interpret the summary and funnel, and investigate unexpected results. The current guide incorrectly assigns clicks and purchases to message delivery date throughout the campaign detail report, describes engagement as tracked clicks alone, and measures time to conversion from individual delivery. The current Rails report groups campaign metrics by each event's recorded time, groups engagement by message dispatch day, and measures time to conversion from campaign launch.

1. Correct the date examples and metric descriptions in Spanish, then adapt the English body.
2. Explain the funnel's engagement count and date grouping without implying a single cohort across all stages.
3. Correct the related date sentence in the completed analytics overview, which repeated the same error; retain its existing links and scope.
4. Check both articles in both languages in the built site on desktop and mobile. The initial text pass did not include a screenshot; the later screenshot follow-up below addresses the reader's request. No message example is needed.

The source check also identified the same obsolete delivery-date claim in the pending Dashboard, data-completeness, and Revenue report guides. They remain pending full bilingual review in later batches; their exact locations are `_i18n/{es,en}/analytics-reporting-attribution/dashboard-guide.md:113`, `data-completeness-and-reporting-gaps.md:44,119,122,166`, and `revenue-report-guide.md:9-13,67` as of this batch.

## Verification and publication

- The report's date and metric descriptions were checked against the Rails source snapshot at commit `449d6ccc46`. `Campaign::Metric` buckets results by `tracked_at` (`app/models/campaign/metric.rb`); attributed purchases enter the campaign metric at the purchase event time (`app/models/attribution/revenue/capture.rb`); the campaign detail uses those event buckets (`app/models/report/calculator/campaign/base.rb`). The separate campaign index can use a source-message cohort and is not the page documented here. CTR and conversion use selected-range deliveries as denominators, so they can show zero in a range with later clicks or purchases but no deliveries (`app/models/campaign/reports/broadcast.rb`, `app/models/report/calculator/campaign/conversion_rate.rb`).
- `app/models/report/widget/campaign/delivery_funnel.rb` combines event-date metric counts with distinct engagement from `Message::DailyRollup`; that rollup uses the business-local dispatch day and includes seen, clicked, or replied messages. `app/models/report/widget/campaign/time_to_conversion.rb` filters attributed purchases by purchase date and measures elapsed time from campaign launch or its available fallback anchor.
- In message rows, the ROI numerator is selected-range revenue, while the estimated delivery cost is for the full message (`app/models/campaign/reports/broadcast.rb`). Segment memberships are snapshotted at purchase time and can overlap, so segment shares need not total 100% (`app/models/attribution/revenue/capture.rb`, `app/models/report/widget/revenue_by_segment.rb`).
- The Spanish body was corrected first and the English body adapted. Both preserve the 11 original article sections and 10 Liquid links, with no missing source targets. The shared stub was unchanged. The related overview correction preserves its 28 Liquid links per language, with no missing source targets.
- `PATH="$HOME/.rbenv/shims:$PATH" BUNDLE_PATH=vendor/bundle yarn build` passed, including `script/verify_security_headers.rb`. The built Spanish and English campaign pages and analytics overviews were reviewed in the in-app browser at 1280px desktop and 390px mobile widths; none had horizontal overflow. The corrected paragraphs, funnel, later sections, related links, and page endings rendered in both languages. This was the initial text-only verification, before the screenshot follow-up.
- The plain local server does not emulate production's extensionless URLs. Source Liquid targets and built `.html` pages were checked instead of testing those links through the static preview.
- The public pages remain at their original publication state. The verifying local commit will be recorded in `progress.csv`; no push, merge, deployment, or public verification occurred.

## Screenshot follow-up (2026-09-26)

The user opened the local campaign article and pointed out that it had no screenshots. This supersedes the earlier text-only visual plan. The last verified text revision is `3678006`; the current Spanish, English and shared source files were copied to `originals/campaign-reporting/pre-screenshots/` before this follow-up.

Reader question: where is the reporting period and how do the four summary cards relate to it? Add one focused, locale-matched static screenshot after the reporting-period instruction in each translated body. Use a fresh, isolated local Rails database and the synthetic `db/seeds/local/campaign_report_showcase.rb` fixture. Capture only the period selector and four KPI cards, excluding contacts, activity and other records. The fixture does not seed engagement rollups, so do not use its funnel as explanatory evidence.

Place each figure immediately after the first paragraph of “Elige el período del reporte” / “Choose the reporting period”. Alternative text should identify the selected first 14 days and the four summary cards in the same language as the page. Keep the caption as HTML and the image as an unlinked `<img>` inside the existing editorial figure classes. Set the maximum CSS width from the verified source pixels at 2× or greater; check whether a separate native mobile capture is needed for readable card labels at 390px.

Capture with the existing ScreenCaptureKit helper from the exact Chrome window. Verify native 2× or higher PNG, embedded Display P3, safe visible data and legible 390px rendering. Keep untouched captures and provenance JSON under `captures/campaign-reporting/`, and published static images under `images/analytics-reporting-attribution/campaign-reporting/`. Do not wrap images in links or add open controls. Rebuild and inspect both articles at desktop and mobile widths; compare built image bytes with source. Record a new verifying local commit, without publication.

The local capture source is ready: an isolated PostgreSQL database named `hellotext_editorial_capture_20260926` was schema-loaded and seeded with the synthetic Hellotext business, then `db/seeds/local/campaign_report_showcase.rb` created the report at `http://127.0.0.1:3190/hellotext/campaigns/4ONLdN32`. Rails uses that isolated database and a dedicated Redis at port 6389; no job worker was started. The rendered Spanish report shows the period selector and four KPI cards with demonstration values. The lower funnel has no engagement rollup fixture and must be excluded. No real contacts, campaigns, or messages were changed or sent.

Automatic approval review initially rejected opening a dedicated Chrome capture window because the standing “no new windows” instruction could include that temporary window. The user explicitly approved a temporary window for Spanish and English capture. The resulting window showed only the isolated demonstration campaign report and was closed after capture. No real contacts, campaigns, or messages were changed or sent.

Four exact-window ScreenCaptureKit crops were recorded as untouched native Display P3 PNGs with provenance JSON under `captures/campaign-reporting/` and copied byte for byte to `images/analytics-reporting-attribution/campaign-reporting/`. Desktop variants show the period selector and all four summary cards: Spanish 3090 × 930 pixels and English 3090 × 1050 pixels. Mobile variants show the selector, first card, and carousel arrow: 754 × 560 pixels in each language. All are native 2× captures. The helper used `--hide-cursor`, but a browser automation pointer was later found over the Spanish desktop Conversion value; the system cursor setting did not suppress that overlay. Every crop was inspected for locale, legibility, demonstration values, and absence of contacts or private data. The source and static asset hashes match for each variant. The original captures were not resampled or color-profile converted.

One unlinked `<picture>` figure was inserted immediately after the reporting-period instruction in each language. Its desktop image is used at 1280px; the mobile source is used at 390px. The HTML caption and alternative text are localized. At desktop width the 3090px source displayed at 608.8 CSS pixels; at mobile width the 754px source displayed at 316 CSS pixels. Both exceed the required 2× density. The figure contains no anchor, button, or focusable open control.

`PATH="/Users/pel/.rbenv/shims:$PATH" BUNDLE_PATH=vendor/bundle yarn build` passed again, including the Jekyll language builds and `script/verify_security_headers.rb`. The four built/site image copies matched the source files byte for byte. The Spanish and English pages were visually reviewed in the in-app browser at 1280px desktop and 390px mobile widths. Each showed the intended locale-specific screenshot in the expected section with readable labels, and neither page had horizontal overflow. The local Spanish preview at `http://127.0.0.1:8765/es/reportes-de-campa%C3%B1a.html` returned HTTP 200; the English preview is `http://127.0.0.1:8765/campaign-reporting.html`.

The temporary Rails, Vite, and Redis processes and Chrome window were closed after verification. The isolated PostgreSQL demo database remains local; the Help preview at port 8765 remains running for review. The verifying local commit is recorded in `progress.csv`. No push, merge, deployment, or public verification occurred.

## Pointer correction (2026-09-26)

After the screenshot follow-up was merged as Help PR 81, the user noticed an unwanted browser automation pointer over the Spanish desktop Conversion value. The former desktop PNG and untouched original had SHA-256 `0b025849a4fa30a823daac11cd84b8ca981955a481c9d342f71934804775d58f`. The English desktop and both mobile images were checked and did not contain that pointer, so their bytes and article markup remain unchanged.

The shared editorial screenshot guide now defaults to no visible pointer. Its only exception is a deliberately enlarged pointer on a clickable button or link, with the tip on the target and its label readable. The native ScreenCaptureKit helper now hides the system cursor by default; the browser automation pointer must also be moved outside the exact crop, followed by visual inspection of the saved pixels. The legacy `--hide-cursor` option still works, and `--show-cursor` is explicit for the narrow exception. The shared rule was merged in editorial PR 2.

The Spanish desktop crop was retaken from the isolated synthetic campaign report in a temporary Chrome window, with Spanish UI and “Primeros 14 días” selected. The browser pointer was moved below the crop before capture. The resulting 3090 × 930 PNG is native 2× Display P3, contains only the period selector and four demonstration KPI cards, and shows no pointer, contacts, or private records. It was copied byte for byte to the original and published-asset paths. The new SHA-256 is `5dd7a7b04202891222d56ac9e92cff65dcdd3a2f7877faf44eac14e193fe052e`; the exact window, rectangle, color profile, pointer treatment, and replacement reason are recorded in `captures/campaign-reporting/summary-period-es-desktop.json`.

The Help build and security-header check passed. The rebuilt image copies and the local server response match the new source hash. In the local Spanish desktop page the corrected figure shows all four cards without a pointer; the Spanish and English articles select their locale-matched desktop and mobile sources at 1280px and 390px, with no linked image controls or horizontal overflow. The temporary demo Chrome window and isolated Rails, Vite, and Redis services were closed after capture; the Help preview at port 8765 remains available. This correction is verified locally here; PR, merge, and public verification are tracked separately.

Help PR 82 merged the correction as commit `4f37a30381e08c074fa459d854646bcb54283bce`. At 2026-09-26 04:55 UTC, the public Spanish and English campaign guides both returned HTTP 200 and referenced their respective desktop and mobile assets. The public Spanish desktop PNG returned SHA-256 `5dd7a7b04202891222d56ac9e92cff65dcdd3a2f7877faf44eac14e193fe052e`, matching the verified local and deploy-preview files. The image replacement is therefore public and verified; the provenance JSON records the same public check.

## Additional report visuals (2026-09-26 plan)

The reader pointed out that the rest of the campaign report has no screenshots. The current Spanish and English article bodies were preserved byte for byte under `originals/campaign-reporting/pre-additional-visuals/` (SHA-256 `57176df23085190a82e35a6a38dbd5320e10ec5830572d52289930b2ba65df71` and `6f15abb0d842c324df292adeb105f5d428bf8950cab8b80de381d53a442f92b6`). The existing figure covers only the period selector and four KPI cards.

Add focused, locale-matched screenshots beside six distinct reader tasks: select a KPI and read its time-series chart; read the four-stage delivery funnel; compare one synthetic message row; read time to conversion; compare attributed revenue by channel; compare attributed revenue by segment. Capture desktop and mobile states separately where the inline desktop crop would make labels too small. Keep the prose, article metadata, links, and publication state. Do not add redundant screenshots to attribution methodology, decision examples, troubleshooting links, or recent activity, which risks exposing contact identifiers.

Use only the isolated `hellotext_editorial_capture_20260926` database and its synthetic campaign, extending the fixture in that database where the current widget would otherwise be empty or inconsistent. Capture each actual UI widget with the native exact-window ScreenCaptureKit helper as 2× or denser Display P3 PNG, without a pointer or private records. Keep untouched originals and provenance alongside the previous captures; copy published variants byte for byte to `images/analytics-reporting-attribution/campaign-reporting/`. Insert static, unlinked figures with localized HTML captions and alternative text immediately after the relevant explanations. Re-read both article bodies before editing, build, check source and built bytes, inspect both complete articles at desktop and mobile widths, then commit and publish through a reviewed PR. Record public verification separately.

## Additional report visuals (2026-09-26 local verification)

- The isolated Rails snapshot `449d6ccc46` and database `hellotext_editorial_capture_20260926` were used only for synthetic report capture. The demonstration report has 5,400 sent messages, 5,133 deliveries, 1,032 tracked clicks, 321 attributed purchases, and $1,872 attributed revenue. Its funnel shows 100%, 95.1%, 28%, and 6.3%; time to conversion shows 9%, 47%, 19%, 25%, and 0%; channel revenue shows WhatsApp 60% and SMS 40%; the synthetic VIP and Q2 2026 segment memberships overlap at 60% and 50%. No campaign or message was sent, and no real contacts or customer records were changed.
- Seven reader tasks now have localized figures per article: reporting period and summary, selected KPI chart, delivery funnel, campaign message metrics, time to conversion, revenue by channel, and revenue by segment. Each has a desktop and mobile variant, for 28 native PNG captures total. The six newly added figures follow the relevant paragraphs. Both article bodies retain their 11 headings and 10 Liquid links; the shared stub, titles, descriptions, slugs, languages, and publication state are unchanged.
- ScreenCaptureKit captured the exact dedicated Chrome window directly to 2× PNG with embedded Display P3. The system cursor was hidden, the automation pointer was outside every crop, and all 28 images were visually checked for locale, legibility, coherent demonstration values, and absence of private data. `captures/campaign-reporting/*.json` records each original, rectangle, window, profile, SHA-256, and verification. The four earlier period captures were replaced so their values agree with the newly populated widgets; the JSON retains the prior public checks as superseded history. The exact logical viewport for the recaptured desktop period image was not retained and is explicitly unverified in its provenance.
- The mobile message image focuses on the metrics because the responsive live row crowds its preview and labels; the desktop companion includes the message preview. The live mobile time chart omits some horizontal axis labels, while the distribution and percentages remain visible. Recent activity has no screenshot because a populated feed could expose customer identifiers. The attribution methodology, decision list, and troubleshooting text do not need separate UI captures.
- Each original matches its static asset, `_site` copy, and HTTP-served local image byte for byte across all 28 variants. `PATH="/Users/pel/.rbenv/shims:$PATH" BUNDLE_PATH="/Users/pel/.codex/worktrees/dad4/hellotext-help/vendor/bundle" yarn build` passed for Spanish and English, including `script/verify_security_headers.rb`. Both complete articles were inspected in the in-app browser at 1280 × 900 and 390 × 844. Every localized image loaded in its intended section; minimum measured density was 2.05×; figures have alternative text and captions but no linked image controls; neither page had horizontal overflow. The local previews are `http://127.0.0.1:8766/es/reportes-de-campa%C3%B1a.html` and `http://127.0.0.1:8766/campaign-reporting.html`.
- Local verification is complete. The verifying commit is recorded in `progress.csv`; PR, merge, deployment, and public asset checks are tracked after this local review.
